using Lox.Errors;

namespace Lox;

/// <summary>
///   Takes Lox source code and runs it.
/// </summary>
public static class Interpreter
{
    /// <summary>
    ///   Runs code.
    /// </summary>
    /// <param name="code">Source code to run</param>
    /// <returns>An error if an error occurred, otherwise, null.</returns>
    public static LoxError? Run(string code)
    {
        return null;
    }
}
